import pickle
# a,b,c = input('请输入：').strip().split()
# print('a:',a,'\nb:',b,'\nc:',c)

# print('\n'.join([''.join(['%s*%s=%-3s'%(y,x,y*x) for y in range(1,x+1)])for x in range(1,10)]))
# print([(['%s*%s=%-3s'%(y,x,y*x) for y in range(1,x+1)])for x in range(1,10)])